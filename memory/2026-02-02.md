# 2026-02-02

## 🚨 服务器宕机事故 — 最严重教训

**时间线：**
- 昨晚某session修改了 clawdbot.json，添加了 browser.profiles.openclaw 但缺少必需的 color 字段
- 服务启动时config validation失败
- 进入 activating (auto-restart) 无限循环
- Jason睡觉期间整晚宕机
- 所有heartbeat、cron、监控、项目推进全部停止
- Jason手动修复后服务恢复（15:20 UTC）

**Jason的反馈：**
> "这次问题非常严重，浪费了大量的时间"
> "为什么这么重要的操作，你没有验证就直接做呢？"
> "我不是跟你说过了无数次，做之前要verify，fact check吗"

**已采取的措施：**
- AGENTS.md 添加铁律第一条（配置修改规则）
- SOUL.md 添加 Verify Before Acting 原则
- MEMORY.md 记录事故详情
- 所有session和subagent启动时都会读到这些规则

**教训：**
1. 永远不要写不完整的配置
2. 改完必须验证
3. 夜间不做高风险操作
4. verify → act → verify，不是 act → hope

## 项目推进
- 15:34 Jason要求全部项目并行推进
- 派出sub-agent: btc-analysis（数据分析）、kalshi-scan（市场扫描）
- BTC Arbitrage: settlement tracker还在运行，6h monitor数据已收集完毕
- Kalshi: paper trading 6笔待结算

## Kanban自动化系统 — 完整记录 (2026-02-02)

### 建立的工具
1. `scripts/parse-status.py` — 解析STATUS.md提取项目信息(JSON)
2. `scripts/validate-status.py` — 格式校验+自动修复
3. `scripts/sync-status-to-kanban.sh` — 主同步脚本（被其他session覆盖过，需要注意）

### 关键发现
1. **Workspace路径映射**: `/workspace` (sandbox) = `/home/clawdbot/clawd/` (host), inode 572460
2. **Docker不follow symlinks**: volume mount只看路径本身，不follow symlink
3. **Tasks.md格式**: 简单markdown（`# 标题` + `## 状态` + `## 描述`），不要用复杂metadata格式

### Kanban架构
- **看板URL**: http://45.55.78.247:8090
- **容器**: 808c0d52b2b2 (Tasks.md, baldissaramatheus/tasks.md)
- **容器挂载**: `/home/clawdbot/kanban/tasks/` → 容器内 `/tasks/`
- **Agent写入**: `/home/clawdbot/clawd/kanban-tasks/`
- **同步方式**: Host cron每5分钟 `rsync -a --delete clawd/kanban-tasks/ kanban/tasks/`
- **列名**: TODO / 进行中 / 暂停 / 完成

### 当前项目状态 (5个)
1. btc-arbitrage → 进行中 (WebSocket基础设施已实现)
2. chinese-social-scripts → 进行中 (核心闭环完成，待积累内容)
3. kalshi → 进行中 (WebSocket基础设施已实现)
4. video-editing → 暂停 (等待测试视频)
5. video-repurpose → TODO (规划中)

### 遇到的问题
1. **脚本被其他session覆盖**: sync-status-to-kanban.sh被替换成stub脚本（只写log不同步）
2. **权限问题**: sandbox以root创建的文件，host上clawdbot用户无法修改
3. **路径硬编码**: parse-status.py默认扫描/workspace，host上不存在
4. **环境变化**: 从sandbox(root@/workspace)变成host(clawdbot@/home/clawdbot/clawd)

### 待修复
- [ ] Jason需要运行: `sudo chown -R clawdbot:clawdbot /home/clawdbot/clawd/`
- [ ] sync脚本需要用相对路径或自动检测workspace
- [ ] parse-status.py的默认路径需要兼容host和sandbox两种环境
- [ ] 防止其他session覆盖关键脚本

### STATUS.md标准格式
```
# STATUS.md — 项目名
Last updated: YYYY-MM-DDTHH:MMZ
## 当前状态: [进行中/卡住/完成/规划中/暂停]
## 最后做了什么
...
## Blockers
...
## 下一步
...
## 关键决策记录
...
```

### 教训
1. Docker volume mount不follow symlink — 用rsync代替
2. 文件权限在sandbox(root)和host(clawdbot)之间会冲突
3. 脚本路径不要硬编码/workspace，用相对路径或$PWD
4. 第三方工具(Tasks.md)要先查文档确认格式要求
5. 关键脚本可能被其他session覆盖，需要防护机制

## Browser自动化功能上线 🌐 (17:17 UTC)

### 完成的配置
- 安装 Google Chrome 144.0.7559.109 (deb包，非snap)
- 配置 Clawdbot browser控制服务 (CDP port 18800, profile: openclaw)
- Headless + noSandbox模式（服务器无GUI）
- Elevated权限已启用（full auto-approve）

### 踩坑记录
1. Snap版Chromium与Clawdbot控制服务不兼容（snap沙箱限制导致CDP启动失败）
2. Gateway进程PATH里没有/snap/bin，创建了软链接也没用
3. 最终解决：安装Google Chrome deb包（非snap版）
4. `systemctl --user restart clawdbot` 不工作 → 用 `gateway restart` 工具
5. browser工具有15秒超时，但控制服务实际需要更长时间启动Chrome

### 配置文件
- `/home/clawdbot/.clawdbot/clawdbot.json` 包含完整browser和elevated配置
- `TOOLS.md` 已更新浏览器使用说明

### 所有session现在可以
- 使用browser工具自动化网页操作
- 截图、填表、爬数据、登录网站
